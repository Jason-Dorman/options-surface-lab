# Backlog — Assignment 2, Covered Call Backtest

The task board for the second assignment. Same rules as [BACKLOG.md](BACKLOG.md): work
top-down within the current milestone; task IDs are one sequence across both boards and are
never renumbered; update status in the session that does the work; **[PO]** tasks are
scaffolded by sessions, completed by the PO. Requirement-level status lives in
[PRD.md Part B](PRD.md) (§13–§20); behaviour in [SPEC-COVERED-CALL.md](SPEC-COVERED-CALL.md).

**Status:** ✅ done · ◐ in progress · ☐ todo · ⊘ blocked (blocker named)

**Due: Sunday 2026-09-20, 11:59 pm EST. First live entry: Monday 2026-09-14** (PO, 2026-09-12).

**Sequencing — under review.** The PO chose restructure-first before the due date was known.
With eight days left, the session **recommends deferring [BACKLOG.md](BACKLOG.md) M5 until
after the submission** and building this page on the current builder (T-79) — **PO to
confirm**. Either way the tape and the engine are pure Python and start now. The order that
fits the calendar: ~~T-62 spike~~ (✅ 2026-09-13) → ~~SD-2, SD-3, SD-5, SD-6 decided~~ (✅ 2026-09-13, T-55) → **T-65 rules + T-80 live leg, dry-run
by Sunday** → ~~T-78 Monday's entry~~ (◐ **booked 2026-09-14**, settles 09-18) → ~~T-56 tape module~~ (✅ 2026-09-14) → ~~T-77 the pull~~ (✅ 2026-09-15) → ~~T-57 engine + invariants~~ (✅ 2026-09-15) → **T-58** → T-79/T-59 page → T-60 write-up →
T-71/T-72 ship by Saturday 09-19.

---

## A2-M0 — Governance and the decisions

| ID | Task | Maps to | Status |
|---|---|---|---|
| T-76 | Governance docs: PRD Part B (§13–§20), `SPEC-COVERED-CALL.md`, this board, AD-12 proposed, precedence tables updated | all | ✅ 2026-09-12 |
| T-62 | **Spike — what LSEG gives hourly on an expired weekly, on QQQ.** Pull `QQQ.O` daily for the window to band strikes, then one recent expired Friday QQQ call near the money hourly over its life (a `QQQI42657000.U^I26`-shaped RIC — 04-Sep-26 expiry, strike from the band; the brief's proven RICs are AAPL, so this **proves the QQQ root and format**), plus `QQQ.O` hourly for the same days; confirm the 09-11 expiry already resolves under the expired form (SD-2). Record: the fields that return, bar timestamp convention (start/end, UTC/local), how far back hourly history reaches for expired contracts, which strikes around it return data (the step). **Writes no cache.** Closes OQ-10 / OQ-11 / OQ-12 and fixes SPEC §3.1's `ts` convention. Can run today — touches nothing on the board. | FR-13, OQ-10–12 | ✅ **2026-09-13.** Evidence: `notebooks/t62_qqq_hourly_spike.json`. QQQ root and format proven. **Bars are tz-naive UTC stamped at the START**, and both tapes run past the 16:00 ET close with real quotes — so the *closing bar* is the 15:00 ET one and `max(ts)` is a post-close stub (SPEC §3.2 item 4). **Strike step $1.00.** Hourly reaches a contract's whole life. Two surprises: the brief's unpadded-`DAY` RIC rule does not resolve (OQ-15, padded is right), and the caret suffix is **not** immediate — 09-11 is still live-form only, so the pull must try both (SPEC §3.3). |
| T-55 | **[PO]** The strategy decisions SD-1 … SD-6 (PRD §15 — options and a recommendation for each): underlying, window, starting cash, entry bar, strike rule, ITM rule. | FR-14, §15 | ✅ **All six closed.** SD-1 **QQQ** (2026-09-12). 2026-09-13: SD-2 **`2026-07-06 → 2026-09-11`**, SD-3 **$75,000 fully funded**, SD-4 **last bar — the 15:00 ET closing bar**, SD-5 **nearest OTM**, SD-6 **strict (`> K`)**. Consequences measured by T-62 and recorded in PRD §15: nearest OTM on a $1 step lands ~0.1% above spot and **6 of 10 weeks assign**; peak entry cost **$72,982** against $75,000, so the book is ~97% invested at the peak and `NEG_AVAILABLE` never fires. Still to do: **print them on the page** (FR-14). |
| T-74 | **[PO]** Sign off AD-12 — one subpackage per assignment (`options_surface_lab/covered_call/`), layered inside. | AD-12 | ✅ **Accepted — PO, 2026-09-14, amended on two points** T-80 established after the proposal: **`live.py` joins the module list** (FR-21 did not exist when AD-12 was drafted), and **the blotter-row constructors are pinned to `rules.py`** so `engine.py` never imports the module holding the network (NFR-5). Audited before signing: `rules.py` is pandas + stdlib only; `live.py`'s sole network seam is `lseg_session()`; `writeup.py` has no imports; **nothing in 1.1 imports `covered_call`**; SYSTEM-SPEC §4's import table already carries both rows. The package docstring had gone stale on its own subject (it called `tape` the only network while `live.py` held it) — fixed. |
| T-78 | **[PO + session] Monday 2026-09-14 — the first entry, booked by the system** (FR-21). **T-81 changed how the settlement half runs — read the Status cell before Friday.** *Must exist before the bar:* ~~SD-2…SD-6 decided~~ (✅ T-55); T-65 and T-80 merged and **dry-run against Friday 09-11's expired chain** on Sunday; Workspace open and logged in **when the command is run**. *This is not a race:* T-62 proved LSEG serves hourly `BID`/`ASK` history for a **live** weekly, so the entry is captured from the **completed** 15:00–16:00 ET bar after it closes — the same completed bar the backtest reads. Run it any time from ~16:05 ET Monday onward, the same evening; the bar is immutable, so the booked trade is identical whenever it runs. Nothing about the decision uses post-16:00 information. *At the bar, one command:* `python -m options_surface_lab.covered_call.live enter` — captures the QQQ print and the near-the-money 09-18 call chain (bid / ask / last, timestamps) into `covered_call_live.json`, selects the strike by the rule, books `BUY 100` at the print and `SELL 1` at the mid, and writes the blotter. If the quote at the bar is invalid it books nothing, logs the skip, and says so — the rule is the rule on day one. *Friday 09-18, last bar:* `… live settle` — captures the settlement print, books `EXPIRE` or `ASSIGN` + `SELL`. | FR-21 | ◐ **Entry booked 2026-09-14; settlement Friday 09-18 remains — and T-81 found two ways it could have gone wrong, both now fixed.** (1) **Nothing tied the capture to the open call.** `--expiry` defaults to `coming_friday()`, which from the Saturday onward is the *next* Friday, and every guard downstream keys on the session — so a settle run that slipped past Friday resolved the 18-Sep call against the 25-Sep close, booked an `ASSIGN` dated a week after the contract expired, and passed every check (reproduced on a copy of the real book). `plan_settlement` now **raises** on a mismatched expiry, and `settle` defaults its expiry to the contract the book is short. (2) **An acquisition failure was recorded as a market fact.** An LSEG timeout, or running before the 15:00 bar closed, wrote `SKIP_NO_STOCK_PRINT` into the book and exited 0 — after which I-10 refused the re-run, leaving the call open forever (I-7) and recoverable only by hand-editing committed data. `unreadable_reason()` now reads the diagnostics `capture()` was already recording and **writes nothing**, exiting 2. Also: settlement reads the **closing** bar explicitly (`bar_kind="closing"`), which is the same 15:00 bar under SD-4 but would have been the 09:00 bar had `entry_bar="first"` ever been chosen (PRD OQ-13 leaves that open).** One run of `… live enter` that evening read the **15:00 ET** bar (the tape ran to 18:00 — `max(ts)` would have been a post-close stub), spot **709.16** → nearest OTM **710**, and booked `BUY 100 QQQ @ 709.16` + `SELL 1 QQQI182671000.U @ 6.045` (the mid of 6.01/6.08), cash **4,688.50**. **I-1 reconciles against the blotter alone**: 75,000 − 70,916 + 604.50. No skips; `ric_form: live`; no diagnostics errors; 97 covered-call tests green. Legs `sync=5s` apart (stock `c_sec_ofst` 3599, call 3594) — trade-to-trade, per SPEC §6.3. **Evidence for the write-up:** the bar's high was **711.96** against its 709.16 close, so reading the bar's *start* would have written **712**, not 710 — the 09-08 dry run's observation-point argument (719 at the close, 720 at the open) repeated on a live week. |
| T-80 | `covered_call/live.py` — **the live leg, by Sunday.** `capture()` pulls `QQQ.O` and the live-RIC chain for the coming Friday (no caret suffix — **T-62 proved the shape: `QQQI182671500.U` returns hourly `BID`/`ASK`**) into the SPEC §3.1 schema, selecting the **15:00 ET closing bar** and never `max(ts)`; `enter()` / `settle()` call the **same** `select_strike` and the same blotter-row constructors the backtest will use — the constructors land here first and T-57's engine reuses them, so a live row and a backtested row come from one code path (NFR-5). State persists in `covered_call_live.json` (committed — it is data the page renders). Tests: a dry run on 09-11's expired chain produces the rows the rule says it should; an invalid quote produces a logged skip and no rows. | FR-21, NFR-5 | ✅ **2026-09-13.** `capture` / `plan_entry` / `plan_settlement` / `apply_rows` + the CLI; **47 tests, 15 of 15 injected defects caught**; RUNBOOK §7 written. The planners are **pure functions of a captured payload**, so the whole leg is tested offline (NFR-4) by faking the one session seam. Dry run against the expired 09-11 chain: entered **Tue 09-08 15:00** at S=718.41, wrote the **719** call at mid **4.80**, settled **714.78** → `EXPIRE`, shares kept — identical to what `rules.py` produces over the same tape. Two guards the mutation run forced: **a re-run is refused** (booked rows *and* an already-logged skip, I-10), and the entry session is resolved **off the tape** (DR-7) — `expiry - 4 days` would have entered on Labor Day and logged a false skip. |

## A2-M0 — Governance and the decisions *(continued)*

| ID | Task | Maps to | Status |
|---|---|---|---|
| T-81 | **Adversarial review of the session that landed T-56 / T-77 / T-63** — six lenses (the pull path, schema and time, the synthetic tape, the live guard, test quality, claims-vs-reality), each finding then attacked by a skeptic told to refute it and to run code rather than reason. | NFR-2, NFR-6 | ✅ **2026-09-15. 31 findings ruled on: 23 confirmed, 8 refuted.** Two were **high, both on the settlement leg** and both reproduced against a copy of the real book before being fixed — see the T-78 row. Also fixed: the contract accounting counted RIC *strings* (952 + 150 = 1,102 became **952 + 75 = 1,027 contracts**, 1,102 RIC requests); the synthetic window's **last week carried a pathology** and so ended before `end_date` for several `weeks=` values; the killed quote was applied **before** its bar's print was drawn, so the fixture's canonical DR-1 row carried a trade at half the contract's value; `lseg_session()` was never proven to **call** the guard added earlier that day (deleting the call left the suite green); and the OQ-6 clock guard's docstring sanitiser **deleted the exact substring** its own check searched for, so its main arm could never fire. **413 tests green; 19 of 21 injected defects caught** across the repair (the two misses are a defensive raise that is unreachable by construction and a draw-order property that no input can currently violate — both noted in place rather than papered over). |
| T-82 | **Adversarial review of T-57** — six lenses (the weekly loop, money and the ledger, time and bars, test quality, claims-versus-reality, layering), each finding then attacked by a skeptic working in an **isolated worktree** and required to run code rather than reason, plus a completeness critic. Run as a 13-agent workflow. | NFR-2, NFR-6 | ✅ **2026-09-15. 37 rulings + 6 completeness findings: 33 confirmed, 4 partial, 0 refuted** — roughly 25 distinct issues, since the serious ones were found independently by three to five lenses each. The top five were reproduced by hand before anything changed. **One high:** the engine **fabricated the contract's RIC** (`build_option_ric(..., expired=True)` hard-codes the caret form) instead of reading the spelling the tape answered under — SPEC §3.3 makes that a function of the *pull date*, and on a live-form tape the blotter named a contract with no bars and every per-bar call mark then missed and carried forward **silently**: 624 of 624 covered bars frozen, NAV wrong on 614 of 784 by up to **$2,201**. I-3/I-4/I-6 catch the blotter half; **the ledger half had no invariant at all**, and the ledger is what FR-16's chart draws. Fixed: the RIC is read off the tape, marks key on `(expiry, strike, ts)`. *Nothing shipped wrong — the committed tape is 952/952 caret — but it was one re-pull away.* **Mediums fixed:** I-7 contradicted §7's carry-forward that the same session had added (widened, teeth kept); `daily_ledger()` **deleted a whole session** with no 15:00 bar, taking `final_nav` with it; the blotter carried `6.130000000000001` in `fill` against its own note's `mid=6.13` (rounding now happens once, in `rules.money()`); `total_return` had **no test** (swapping NAV for cash — +1.66% to −93.6% — left the suite green); four of `weekly`'s columns were unguarded; a tape for another underlying was accepted and produced a complete, plausible, entirely wrong book. **Three doc numbers were wrong:** `$38,236` for the available floor came off a **05:00 pre-market** bar (the flag is only checked at entry bars — **$38,886.50**), "five skip paths" is four reasons over five weeks, and CLAUDE.md still said 413 tests. **Two things carried rather than fixed:** the guard written to enforce S-7 was itself T-46's defect (it looked the mark up by the same key the ledger had used, so it passed with all 624 frozen), and **my own replacement guard let the mutation through** — it checked the blotter's RICs against the *union* of both tapes, which contains the defect; caught only because the repair was mutation-checked in turn. **Flagged to the PO, not fixed:** NFR-5's "one code path" is narrower than it reads — the live leg guesses RICs rather than reading a chain, so it cannot tell *listed but unquoted* from *never listed* and writes the next strike up where the backtest skips the week (SPEC §5 now records it as a stated limitation); and whether §7's carry-forward should bind the live leg. **83 engine tests; 42 defects injected across both passes, 41 caught** — the single miss is unreachable by construction and named in place. **496 tests green, no xfail.** |

## A2-M1 — The tape

| ID | Task | Maps to | Status |
|---|---|---|---|
| T-56 | `covered_call/tape.py`: calendar from the stock tape (weeks, first/last sessions, expiries — SPEC §3.2); chain per week on the **$1.00** step T-62 discovered; batched fail-soft pull for each contract's life, **trying the live RIC form and the caret form and recording which won per contract** (SPEC §3.3); `covered_call_tape.parquet` with the SPEC §3.1 schema + payload keys; `load_tape()` cache-first, `fetch_tape()` refuses to overwrite, `OSL_OFFLINE` honoured; **never touches `option_pipeline_data.pkl`**. RUNBOOK §7 written with it. | FR-13, AD-1, AD-2 | ✅ **2026-09-14.** `fetch_tape` / `load_tape` / `Tape` / `strike_band` / `describe` + the `fetch`\|`inspect` CLI; **34 tests, 19 of 19 injected defects caught**; procedure is **RUNBOOK §8** (§7 is the live leg). The calendar helpers were already in `rules.py` (T-65), so this task is the pull and the file. Four things the spec had left open: the band is per **week's own range** (a window-wide ladder asks each week for the other's strikes); the ladder is arithmetic in **integer hundredths**, because a float ladder drifts into a RIC one cent off a real contract and that returns *nothing* rather than erroring; `load_tape()` reaches the network **under no circumstances** — only `fetch_tape()` does, and it refuses under `OSL_OFFLINE` — so NFR-4 is a property of which function you called; and a pull that finds no stock **writes nothing**, since a half-written tape blocks its own retry. Payload keys live in a JSON **sidecar** (`covered_call_tape.meta.json`), committed with the parquet. `pyarrow` is now in `requirements.txt` — CI installs from it. ~~**Open:** the synthetic fallback is T-63~~ — **landed 2026-09-15**: `load_tape()` falls back to the synthetic tape with a `RuntimeWarning`; `fallback=False` raises instead. |
| T-63 | `synthesize_tape(seed, end_date)` — explicit `end_date` (OQ-6's lesson), the properties in SPEC §3.4, at least one skip per window, one holiday week, both OTM and ITM Fridays. Fixture in `tests/covered_call/conftest.py`. | AD-7, NFR-4 | ✅ **2026-09-15.** In `tape.py` beside the loader it backs; `tests/covered_call/conftest.py` carries `synthetic_tape`, `role_weeks`, `params` and a `real_tape` that **skips** rather than silently synthesizing. **29 tests, 17 of 17 injected defects caught.** `end_date` is **required and positional** — a test asserts it has no default and that no clock function appears in the generator's source, which is OQ-6 made mechanical. Every skip reason the engine can log has a **named week** (`SYNTHETIC_ROLES`), checked by reading it back through `rules.py` rather than against the generator's intent. Two things the mutation run forced: option prices must come from **each bar's own spot** (pricing off one fixed level passes every static bound while destroying the mid↔spot relationship T-57 and T-58 read — the guard recomputes Black-Scholes from the tape's own stock column), and `load_tape`'s fallback must **warn**. Also corrected SPEC §3.3: `OSL_OFFLINE` means *never pull*, not *use the synthetic tape* — CI sets it on every build, so the literal reading would have published a fabricated book. |
| T-64 | Tape tests: schema and dtypes; `mid` NaN on every invalid-quote case (SPEC §6.1); calendar derivation on a synthetic week with a Friday holiday; loader offline; fetch refuses an existing cache. | FR-13, NFR-2 | ✅ **2026-09-15**, completed by T-63's half. **`test_tape.py` (34) + `test_synthetic_tape.py` (29), 34 of 34 injected defects caught across the two.** Was ◐ **with T-56** — `tests/covered_call/test_tape.py`, 31 tests over a faked LSEG seam: the stored schema and dtypes, `mid` against the scalar `valid_mid` on ten awkward quotes, a **Friday-holiday week expiring Thursday with the RIC carrying that date**, the loader opening no session, both refusals, the batch retry, and the two RIC forms. What waits on T-63 is the same suite over a *synthetic* tape rather than a faked pull. |
| T-77 | **[PO + session]** The one-time hourly pull per **RUNBOOK §8**, verify diagnostics, commit the parquet **and its `.meta.json` sidecar**. Same discipline as T-7. | FR-13 | ✅ **2026-09-15.** `covered_call_tape.parquet` + `covered_call_tape.meta.json` — **37,857 bars, 952 contracts, 10 weeks, 2026-07-06 → 2026-09-11**, `synthetic=False`. The first attempt (09-14) was blocked on the desktop, not the data: the API proxy answered `ST_PROXY_READY` while `POST /api/handshake` timed out on every retry (RUNBOOK §9). **Verified against something it did not produce** — run through `rules.py`, the tape reproduces all ten 15:00 ET entry bars, W37 entering **Tuesday 09-08** off the tape (Labor Day, DR-7), and **6 of 10 weeks assigning**, matching the independent script behind T-65; W37's entry (S=718.41 → K=719 @ mid 4.80) is **identical to T-80's dry run**, which reached LSEG by a different code path. 98.5% of option bars at the ten entry bars carry a valid mid. Accounting is exact: **952 answered + 75 refused = 1,027 contracts**, over 1,102 RIC requests across the two forms (first recorded here as 952 + 150 = 1,102, which counted RIC strings — corrected by T-81). Three findings, all recorded: the **caret changeover is a few days wide** (09-11 answered *live* on 09-13, *caret* on 09-15 — all 952 came back under the caret, so a re-pull on another date yields a different `ric_form_used` map); thin extended-hours bars carry **erroneous `LOW_1`/`HIGH_1` ticks** (09-11 17:00 ET prints a low of 667.36 against a ~715 tape) which widen the band to ~100 strikes a week — the right failure direction; and the seven `TypeError: 'UniverseContainer' object is not subscriptable` batch failures were all recovered by AD-2's single-RIC retry. |

## A2-M2 — The engine

| ID | Task | Maps to | Status |
|---|---|---|---|
| T-65 | `covered_call/rules.py`: `Params` (SPEC §2), `select_strike` for `nearest_otm` (P0), the calendar helpers. Tests incl. the property in I-9 and the ATM tie. | FR-14 | ✅ **2026-09-13.** `Params` frozen, defaulting to the six SD decisions; `select_strike`, `is_itm` (SD-6 strict), `trading_weeks` / `entry_bar_ts` / `closing_bar_ts`. **37 tests, 9 of 9 injected defects caught.** Driven against the real tape: it reproduces all 10 entry bars, strikes and outcomes that an independent throwaway script produced — W37 correctly enters **Tuesday 09-08** (Labor Day, DR-7) and **6 of 10 weeks assign**. `closing_bar_ts` is the only place allowed to decide which bar is "the close"; `max(ts)` is banned in the module docstring and a test injects it. |
| T-57 | `covered_call/engine.py`: `run_backtest(tape, params) -> Book` — the weekly loop (SPEC §4), fills (§6), settlement (§7), blotter + skip log (§8), ledger + Reg T (§9). **Tests before figures:** the I-1 … I-12 invariant suite written first, over the synthetic tape and then the real one; each mutation-checked. One week hand-checked against raw quotes in the test's docstring. | FR-15, FR-16, NFR-6 | ✅ **2026-09-15.** `run_backtest` / `window_weeks` / `build_ledger` / `settlement_print` + the frozen `Book` (blotter, skips, ledger, weekly, diagnostics). **83 tests; 42 defects injected across the two passes, 41 caught** (T-57's own 31, plus 11 new ones in T-82's repair — the single miss is unreachable by construction and named in place). On the committed tape: **10 weeks, 10 entries, no skips, 6 ASSIGN / 4 EXPIRE, $6,657.50 premium, final NAV $76,243.50 (+1.66%)**; all **four** skip reasons fire on the synthetic tape, over five skipped weeks. **2026-09-08 → 2026-09-11 is hand-checked** in a test docstring against the raw bars, and reproduces T-80's live rehearsal — the same entry reached by the other code path. Four things worth carrying: (1) **the spec had no answer for a missing print at the *expiry* closing bar** and the engine needed one — entry may skip because nothing is owed yet, settlement may not because I-7 says every open call resolves, so it carries the last print in that session at or **before** the close and the blotter row says `S_exp_carried`; reaching *forward* would resolve a contract against T-62's post-close stub. (2) **`Book.final_nav` reads the last 15:00 row, not `ledger.iloc[-1]`** — the stock tape runs to 19:00 ET, and on this tape the stub and the close differ by $10.65. `max(ts)` one layer up. (3) **Three of the four first-pass mutation survivors were defects in the guards**, not the engine: I-11 multiplied by the module's own `IM_RATE` and passed at any rate; the combo-skip test picked its week by *role* and watched one that was never flat; and `S_exp == K` happens on neither tape, so SD-6's decision was untested until a test built the case. (4) **$72,982, not $72,984** — the peak entry cost recorded by T-55/T-62 was $2 out; the 2026-08-17 15:00 bar prints 729.82. |
| T-66 | Notebook `03_covered_call.ipynb` §1–§4 co-built with T-57: one week walked end to end with the raw bars beside the blotter rows; the skip log explained; the NAV identity shown. Stored outputs (T-25's lesson). | AD-3 co-build | ☐ **not started — T-57 landed without it** (2026-09-15). The material it needs exists: W37 is walked end to end in `test_engine.py`'s hand-check docstring, and `Book.weekly` is the week-by-week table. |
| T-67 | `occ_symbol()` beside the RIC grammar in `option_surface_utils.py`; round-trips with `parse_option_ric`; test. | FR-15 | ✅ **2026-09-13**, pulled forward because T-80's blotter needs the column. Also added `build_option_ric(..., expired=False)` for the **live** RIC form, and a guard: a strike over **$999.99** no longer builds a RIC that parses back to a *different* contract and returns nothing — it raises. QQQ at ~$715 is well inside, but that failure mode is invisible, which is why it is refused rather than logged. |

## A2-M3 — The evidence

| ID | Task | Maps to | Status |
|---|---|---|---|
| T-58 | `mid_vs_print(tape, band)` + OLS/R² in the transform core (SPEC §10); notebook §5; a test on a synthetic tape with known noise recovers the planted slope. | FR-17 | ☐ after T-56 |

## A2-M4 — The page (waits on T-79 — the interim second output; T-51's generator follows the submission if the PO confirms)

| ID | Task | Maps to | Status |
|---|---|---|---|
| T-79 | **Second output from the current builder** — the interim for AD-11: lift `_panel` / `_readout` / the page shell out of `build_preview.py` into a small shared module both builders import; a `build_covered_call()` writes `_site/covered-call/index.html`; `pages.yml` uploads the directory; the publish guards run per page; `test_build_preview` still green. No registry, no templates — those are T-51, after 09-20. | AD-11 (interim), FR-18 | ☐ |
| T-68 | **[PO taste]** Theme additions: table rules in `PAGE_CSS`; tokens for the NAV / IM / MM lines (amber stays type, never data — DESIGN-BRIEF §3 rule 6); DESIGN-BRIEF §9 records them. | FR-18, AD-6 | ☐ |
| T-69 | `covered_call/plots.py`: NAV/IM/MM path with hover (hero height), the mid-vs-print scatter with fit and `y = x`; captions via `with_caption`; every figure declares its panel height. | FR-16, FR-17 | ☐ after T-68 |
| T-59 | `covered_call/page.py`: `build_page(tape, params) -> Page`, registered at `/covered-call/`; panels in SPEC §11 order; blotter, skip log and ledger as HTML tables; strategy + simplifications printed from `Params`; the FR-14 sentence-pins-params test; CI guards per SPEC §11; I-13; the **Live book** panel (FR-21). | FR-14, FR-18, FR-21 | ☐ after T-79 |
| T-70 | `covered_call/writeup.py` scaffold — the FR-7 mechanism: `[unwritten]` slots for the five write-up questions, red on the page, failing test, CI refusal. | FR-19 | ◐ **module landed 2026-09-14** with the five `[unwritten]` slots and the PO's **methodology block** (written by the PO in the T-80 review): the Rule/Fill/Stock/Audit/Limitation hierarchy, the observation-point paragraph and the midpoint-evidence caveat. `test_writeup.py` **re-runs `select_strike` on the two prices the paragraph quotes** so the prose cannot drift from the rule, and forbids the word NBBO anywhere in it. Still to wire: red rendering + CI refusal, which need the page (T-79/T-59). |

## A2-M5 — Write-up and ship

| ID | Task | Maps to | Status |
|---|---|---|---|
| T-60 | **[PO]** The write-up: strike choice; wait-through-expiry; fill at mid (cite the R²); why Reg T; the analysis — what happened, where theory met tape, what to change. | FR-19 | ☐ |
| T-71 | Browser drive of the built page (RUNBOOK §5 posture): 14 widths, tables scroll rather than deform, hover values on the NAV chart, zero page/console errors; incognito check of the Pages URL. | FR-18 | ☐ after T-59 |
| T-72 | Docs lockstep + PRD §20 definition of done; **[PO]** Canvas submission. | all | ☐ |

## P1 — after every P0 line above is green

| ID | Task | Maps to | Status |
|---|---|---|---|
| T-73 | One comparison variant, not both: `strike_rule = "delta"` (30Δ from the 1.1 inverter) **or** a buy-back at 50% of premium — a second `Params`, a second book, one comparison figure. The write-up gains a paragraph. | FR-20 | ☐ |
