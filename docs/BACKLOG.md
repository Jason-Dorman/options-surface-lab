# Backlog — Options Surface Lab (Assignment 1.1)

The task-level build board. Requirement-level status stays in [PRD.md](PRD.md) §3/§12;
this file is the ordered "what's next" for every session.

**Rules:** work top-down within the current milestone; task IDs are stable (never renumber);
update status in the same session as the work (lockstep rule); PO-owned tasks are marked
**[PO]** — sessions may scaffold them but not complete them.

**Status:** ✅ done · ◐ in progress · ☐ todo · ⊘ blocked (blocker named)

---

## M0 — Foundations

| ID | Task | Maps to | Status |
|---|---|---|---|
| T-1 | Docs suite: PRD, SYSTEM-SPEC, ARCHITECTURE, CLAUDE.md | — | ✅ 2026-08-29 |
| T-2 | FR-1 restructure: package layout, rxconfig, entry shim, anchored paths, .gitignore (incl. app-key), requirements.txt, seed parsing tests | FR-1, G-1/2/7/8 | ✅ 2026-08-29 |
| T-23 | Exploration notebook `notebooks/01_data_exploration.ipynb` — consumes the package only; feeds FR-7 sentences + T-7 verification | AD-3 | ✅ 2026-08-29 |

## M1 — Runnable + real data (before Class 2 checkpoint)

| ID | Task | Maps to | Status |
|---|---|---|---|
| T-3 | Install pytest into `algo` env; confirm `pytest` runs the seed tests green | NFR-2 | ✅ 2026-08-29 — pytest 9.1.1 was already present in `algo`; suite green |
| T-4 | FR-3 transform tests: flatten (both MultiIndex orders), pivot, sparsity stats, nearest-prior-spot join — seeded synthetic panel as fixture. **Gate for all refactors.** | FR-3, NFR-2 | ✅ 2026-08-29 — `tests/test_transforms.py` (34) + `tests/conftest.py` fixtures; 40 green, 1 xfail (PRD OQ-5) |
| T-5 | First `reflex run` smoke test (scaffolds `.web/`, downloads JS toolchain): page loads, figures render, controls work. Completes FR-1 acceptance. | FR-1 | ◐ 2026-08-30 — ran; page + candlestick + controls OK, `.web/` scaffolded. Surfaced two defects (T-26, T-27); the option figures were never exercised against a populated panel. **Re-run once on the synthetic panel to close.** |
| T-26 | Make the cache-first pull *visible*: status badge names the path taken, amber callout + spinner while a pull runs, button disabled mid-load. `OSL_OFFLINE=1` forces synthetic for CI/export (NFR-4); `fetch_from_lseg()` refuses to overwrite an existing cache. Callout also fires when a panel has no settle side. | FR-2, NFR-4, AD-9 | ✅ 2026-08-30 |
| T-27 | Recover SETTLE and puts. **Puts solved 2026-08-30**: suffix takes the *call* month letter for both rights (`UUUUR122601100.U^F26`); 146 puts recovered, now the `build_option_ric()` default. **SETTLE still absent** — 0 non-null across 294 series. | FR-2, FR-6, AD-9 | ◐ |
| T-30 | Re-probe the mark field. **Answered 2026-08-30:** `SETTLE` is absent for expired US equity options under all 7 candidate names and is not in the field list these RICs return; control shows `SETTLE` works for `CLc1` in the same session. Available marks: `MID_PRICE`/`BID` 46.8%, `ASK`/`THEO_VALUE` 50.0%, `OPINT_1` 41.5% vs `TRDPRC_1` 36.8%; **121 of 920 contract-days have a mark and no print**. Exhibit + captured evidence: notebook 01 §10. | FR-2, FR-6 | ✅ 2026-08-30 |
| T-33 | Web/doc search for an alternative settle route — **closed 2026-08-30, no route exists**: `TR.SETTLEMENTPRICE` is futures-only, `TR.OptionSettlementPrice` does not resolve, `TR.CLOSEPRICE` is `TRDPRC_1` re-served (identical 356/356 after dedup; raw rows are padded repeats). LSEG's own expired-options article uses BID/ASK/TRDPRC_1. Notebook 01 §10a. | FR-2, FR-6 | ✅ 2026-08-30 |
| T-34 | Establish *why* no settle exists — **closed 2026-08-30**: no official close/settlement for US listed equity options is published by the exchanges, OPRA or the OCC. Not a data-access problem; every mark is derived (mechanical vs theoretical). Notebook 01 §10b. | FR-6, FR-7 | ✅ 2026-08-30 |
| T-32 | Adopt `MID_PRICE` as the mark. **Done 2026-08-30:** re-pulled with `TRDPRC_1, MID_PRICE, BID, ASK, OPINT_1` (296 series); wide-table slot renamed `SETTLE`→`MARK` with `has_mark`/`n_mark_only`/`pct_mark_no_trade`; plot + app labels name the source field; FR-6, AD-9, SYSTEM-SPEC updated. **Sign-off received 2026-09-01** — the revised README names `MID_PRICE` as the mark. | FR-6, AD-9 | ✅ |
| T-35 | Stop `pivot_trade_settle()` deleting rows with an unknown spot (checkpoint_audit §1) — pivot on `(date, ric)`, re-attach descriptors by merge. xfail removed, test strengthened. | AD-9, FR-3 | ✅ 2026-08-30 |
| T-36 | Default as-of = busiest date, not last. These are expired weeklies: the final date has one expiry alive (33 quotes, 6.1%) vs 216 quotes / 7 expiries mid-window (2026-07-10). Neutral criterion, every date still selectable. | FR-4, FR-6 | ✅ 2026-08-30 |
| T-40 | Adversarial review of the session's changes (15 agents, 37 candidates, 5 verified, 1 survived). Found: the demo script and audit doc quoted the median relative gap as **2.8%** — that was the *superseded* cache's as-of-2026-08-21 slice, carried into a panel-wide sentence. True panel figure is 4.6%; the on-screen card's 4.0% is the as-of slice and is correct. Also corrected an open-interest pair that mixed two row populations (166 vs 28 → 157 vs 28). No code defects survived review. | NFR-2 | ✅ 2026-08-31 |
| T-39 | Bid-ask spread as the trust layer: `spread`/`spread_pct` on the wide table, `median_spread`/`median_spread_pct`/`pct_spread_over_half` in `summarize_sparsity()`, `spread_heatmap()` figure + page slot + preview + metric card. Answers "can I believe this mark?" where occupancy only answers "does a number exist?". Sets up 1.2's fills — filling at mid is optimistic by ~half the spread. Note: keeping BID/ASK adds 244 one-sided quote days to the FR-6 denominator (7,214 → 7,458, 22.2% → 21.5%); a contract quoted with an ask but no bid was still listed. +5 tests. | FR-6, AD-9, FR-11 | ✅ 2026-08-31 |
| T-38 | `surface_grid()` crashed with `QhullError` on a single-expiry slice (flat cloud — the last date of a weeklies panel always is). Degenerate input now returns no sheet instead of raising, per AD-9. This killed `build_preview.py` outright. +3 tests. | AD-9, FR-3 | ✅ 2026-08-30 |
| T-37 | `tests/test_app_figures.py` — smoke tests over the app→plot call sites. Added after a kwarg rename desynced them with no test failing. | NFR-2 | ✅ 2026-08-30 |
| T-31 | **[PO]** Instructor question: the README's expired-contract suffix rule ("repeats the month letter", `^R26` for puts) returns **zero** data; puts require the call letter (`^F26`). Demonstrable live — both forms are one `build_option_ric(..., put_suffix=)` call apart. | — | ☐ |
| T-29 | Acquisition hardening (found while staging T-27): normalise all four `get_history` column shapes to `(RIC, Field)`; stop the per-RIC fallback losing identity and being deduped away; keep requested-but-empty fields instead of collapsing the frame; `build_option_ric`/`build_candidate_rics` in utils as the tested inverse of `parse_option_ric`; additive `diagnostics` payload key. +29 tests (`test_ric_building.py`, `test_acquisition.py`), 69 green. | FR-2, AD-2, AD-3, NFR-2 | ✅ 2026-08-30 |
| T-6 | **[PO]** Pre-flight: confirm UUUU had no split in the 12-week window (OQ-3); if split, pick a new root and update constants | FR-2 | ✅ 2026-08-31 — **no split.** Three checks: largest overnight move +10.2% (a 2:1 split reads ~0.50); strike grid clean on the $0.50 step, $10.50–$18.50, across every expiry (adjusted contracts produce off-grid strikes); and every open-market Friday returned option data — the only gaps are 06-19 (Juneteenth) and 07-03 (Independence Day observed), both market closures. PO closed on this evidence. |
| T-7 | **[PO + session]** One-time LSEG pull per [RUNBOOK.md](RUNBOOK.md) §3; verify payload; do NOT commit until T-8 exists and `.gitignore` is confirmed active | FR-2 | ✅ 2026-08-31 — 296 series (148 calls + 148 puts), `TRDPRC_1, MID_PRICE, BID, ASK, OPINT_1`, committed in `afe99ca`. **Ran out of order:** the first pull fired unsupervised during T-5 *before* T-6's split check; T-6 has since confirmed no split retroactively, so the committed panel is sound. Two superseded pulls kept locally as evidence. |
| T-8 | `git init`, first commit, create GitHub repo, push. Verify `lseg-data.config.json` is untracked **before** the first push. | FR-9 (partial) | ✅ 2026-08-31 — `Jason-Dorman/options-surface-lab`, commit `afe99ca`, 31 files. Secret verified absent from all history. Committed binaries: `option_pipeline_data.pkl` (876 KB) + `.synthetic.pkl` (601 KB) + `options_surface_preview.html`; the two superseded evidence pulls are gitignored and kept locally. |
| T-9 | De-risk deploy early: skeleton `reflex export` + Pages workflow with base-path config; incognito check. (Risk table: do NOT leave this for Sep 04.) | FR-9 | ✅ 2026-09-03 — **incognito verified by the PO**: all six figures render, the as-of slider drives the whole page, the legend toggles series. The `reflex export` blocker below was resolved by T-41's switch to a static page built from the pickle; this row's original plan is superseded, its goal is met. *(Historical note follows.)* ~~◐ 2026-08-31 — **plumbing done, page blank.**~~ CI green: pytest in a clean no-credentials container (NFR-4 proven), `reflex export` on Node 22, base path correct, deploy live at https://jason-dorman.github.io/options-surface-lab/. But the bundle bakes `ws://localhost:8000/_event`; on Pages hydration fails and the tree unmounts. **Blocked on the instructor** — DEMO-SCRIPT q5. |
| T-41 | **[PO]** Resolve the deployment model with the instructor. | FR-9, AD-4, AD-5 | ✅ 2026-09-01 — **(c) static page.** Pages serves one self-contained `index.html`, built from the committed pickle at build time; no backend, no run-time pickle read. Workflow reworked to `build_preview.py`. Consequences: T-14 superseded, T-15 promoted to critical path, `frontend_path` removed. |
| T-24 | Checkpoint exhibits in notebook 01 (§8): live RIC-grammar demo + settle-no-print table; notebook is the demo deep-dive tab | AD-3 | ✅ 2026-08-29 |
| T-10 | Checkpoint prep + demo | M1 | ✅ 2026-09-01 — demo delivered. **Instructor conceded SETTLE is not available at the endpoint** (took some convincing; the evidence pack did the work). What replaces it is still outstanding — see T-32/T-42. |

> **Note 2026-09-01 — provenance of four constraints.** The revised README (Canvas only, no
> `.md` provided) dropped its "What the 3D plot is doing", "Files in this lab", "Stretch (not
> required)" and "Do not" sections. **[PO] elected to keep them as build constraints anyway.**
> They are therefore *our* standards now, not the instructor's: the `*app.py` / `*utils.py` /
> `*plot.py` layout, the interpolation warnings, and the do-not list all still bind, but cite
> them as project decisions rather than as assignment requirements.

## M2 — Rubric complete (~Sep 02)

| ID | Task | Maps to | Status |
|---|---|---|---|
| T-42 | With SETTLE confirmed unavailable, what fills the mark slot? | FR-6, AD-9 | ✅ 2026-09-01 — **`MID_PRICE`, named by the revised README.** The brief now defines it as the closing NBBO midpoint and states LSEG exposes no settlement price for these contracts. No code change: `MARK_FIELD_DEFAULT` was already `MID_PRICE`. Labels that still say "mark" may now say "mid". |
| T-11 | Display the settle-no-trade **percent** on the page (currently computed, not shown) | FR-6, G-3 | ✅ 2026-09-01 — "Mark, no print" card now shows count **and** percent in both the Reflex app and the published page. Field-agnostic: it reads from the MARK slot, so it re-computes if the mark field changes. |
| T-12 | Commentary block under the 3D figure; **[PO]** writes the three sentences (sessions scaffold placement only) | FR-7, G-4 | ☐ |
| T-13 | **[PO decision]** Pick graphical-identity direction (create DESIGN-BRIEF.md at that point); then build `theme.py`, strip all hardcoded hex/fonts from plot + app, verify settle/trade stay distinct | FR-8, G-5 | ✅ 2026-09-02 — **[PO] chose a deep-navy terminal: amber type, Space Grotesk + JetBrains Mono, and a numbered-panel grid layout.** [DESIGN-BRIEF.md](DESIGN-BRIEF.md) records it; `theme.py` implements it. All 138 hex/font literals gone from `option_surface_plot.py`, `options_surface_app.py` and `build_preview.py` — `tests/test_theme.py` greps for them, so the acceptance criterion is executable rather than eyeballed. README's cyan-mark / magenta-print encoding kept and asserted by hue; puts take the hues furthest from their own call (violet for the mark, green for the print) with the glyph encoding role not right (circle = mark, diamond = print), all markers filled; the mark-vs-print scatter draws named Calls/Puts traces with a legend rather than one array-coloured trace. Amber is type only, never data — asserted. Layout replaced the single scroll of full-width cards with a command bar, a readout strip and numbered panels on a hairline grid, 10-column grid — hero surface at 6 cols with the underlying beside it at 4 for spot context (PO's call, 2026-09-02; started 7/3, widened to 6/4 the same day), remaining four figures 5+5 (`as_panel_figure()` hands each tiled figure's title to its panel header). Reflex app and published page render the same chrome. **First pass was corrected by the PO on both counts** — "Bloomberg" meant layout, not palette, and the ice-blue type had to go; see DESIGN-BRIEF §8. WCAG AA measured and asserted on every rendered pairing. **Consequence:** the published page now reaches Google Fonts as well as the Plotly CDN — the self-containment guard was widened to those two hosts (DESIGN-BRIEF §7). +42 tests, suite now 144 green with T-42. **PO review 2026-09-02 caught three defects** — unreadable put markers, puts that read as missing on the scatter, and dead vertical space in the hero row; all three fixed and pinned by tests (DESIGN-BRIEF §8). |
| T-14 | ~~Import-time baking into State defaults~~ | AD-4 | ⊘ **superseded 2026-09-01.** Existed only to populate Reflex State for the static export. The published artifact is now a build-time-rendered HTML page with no Reflex runtime, so there is no State to bake. No work required. |
| T-42 | Wire the as-of slider to the whole page, not just the hero | FR-4, AD-5 | ✅ 2026-09-03 — **[PO] chose full cross-filtering** over labelling the pinned date. `asof_frames()` embeds per-date arrays for the scatter, spread and both occupancy grids plus the six readout values (+34 KB gzipped, ~32 s build); one inline listener applies them on `plotly_sliderchange`. Built by running the real builders per date, so the payload cannot drift from the figures. `settle_vs_trade_figure` now emits empty traces rather than skipping, making trace order a contract. Page gains its first custom JS — fails safe to the old behaviour. +6 tests, CI greps for payload + listener. |
| T-15 | Static interactivity for the published page | FR-4/5, AD-5 | ✅ 2026-09-01 — `static_surface_figure()`: **an as-of slider over all 53 trading days** (308 pre-built traces) plus legend toggles for MID_PRICE / TRDPRC_1 / the interpolated sheet, calls and puts. Puts open as `legendonly`. Date is a slider and the right is on the legend *because* Plotly buttons apply a fixed visibility array and cannot read another menu's state — two menus would fight; the legend is orthogonal. **[PO] chose full coverage over page weight: 2.4 MB, ~308 traces.** `curated_asof_dates()` remains the trim lever if that proves too slow. +8 tests. |

## M3 — Stretch (committed; ~Sep 03; cut before M4 slips)

| ID | Task | Maps to | Status |
|---|---|---|---|
| T-16 | Moneyness axis toggle (K ↔ K/S) incl. static variant; add a moneyness section to notebook 01 (co-build policy) | FR-10 | **✅ 2026-09-04** — see [PRD FR-10](PRD.md) for the acceptance record. `x_mode=` behind a select in the Reflex app; a Plotly dropdown inside the hero on the published page (no event handler runs there — AD-5). Composes with the as-of slider because the two write disjoint properties (`x` vs `visible`) — the rule T-15 could not satisfy. SPEC §12's "pre-rendered trace pair" retired by the PO. Sheet is *rescaled*, not re-interpolated. **Verified in a real Chromium on the built page.** Chip moved inside the plot area (PO, same day — it overlapped the caption/legend/modebar band). Notebook §6a co-built. +10 tests. |
| T-17 | IV inversion in utils (+ round-trip test) + IV figure with assumptions caption; **[PO]** picks the constant rate (OQ-2) | FR-11 | **✅ 2026-09-04** — see [PRD FR-11](PRD.md) for the acceptance record. **[PO] closed OQ-2 at `r = 4.00%`** (short T-bill) and chose the 3D-scatter form over a heatmap or a smile plot, full width as panel **[7]**. `bs_price` / `iv_refusal` / `implied_vol` / `attach_implied_vol` in utils; `iv_surface_figure` in plot; panel 7 in both the app and the published page, following the as-of slider like everything else (a new `_cloud()` lifter and `CLOUDS` branch in the listener — trace order `[Calls, Puts]` is now a contract, as `settle_vs_trade_figure`'s already was). **The refusals are the feature:** 6,275 of 7,458 contract-days invert (84.1%); the other 1,183 are absent from the figure, not filled in, and `iv_refusal` names why (586 no mark, 296 expiry day, 293 sub-intrinsic, 8 bracket misses). Page 2.87 → 3.00 MB. **Two defects were visible only in the built page** (the pattern this project keeps hitting): `as_panel_figure` swapped in the 30px tile margin and clipped FR-11's assumptions caption clean off the canvas — an annotation above the paper stops drawing without erroring, so the deliverable lost its rate and day-count while the suite stayed green — and the hero's near-cubic 3D scene left the cloud adrift in a 2.6:1 box. Fixed with `WIDE_FIGURE_MARGIN` / `SCENE_ASPECT_WIDE` / `SCENE_CAMERA_WIDE` (`as_panel_figure` now takes a margin), and the caption one is now pinned by a test that does the arithmetic for every panelised figure — it found `spread_heatmap` sitting at 28 of its 30 available pixels. **Driven in a real Chromium** per RUNBOOK §5: panel [7] follows the slider, keeps `[Calls, Puts]`, spans all ten columns, no page or console errors. **Then T-44's adversarial review found two more that browser check had missed** — the caption and the hover were still on the build date. Fixed and re-verified. +44 tests (`tests/test_iv.py` + figure/page guards), 198 green. |
| T-25 | `02_iv_surface.ipynb` co-built with T-17: where inversion degenerates (sub-intrinsic, near-expiry), rate sensitivity | FR-11, AD-3 | **✅ 2026-09-04** — `notebooks/02_iv_surface.ipynb`, 29 cells, **every code cell executed top-to-bottom on the real panel** before landing. §2 round-trips one real contract in the open and plots the price-vs-vol curve the solver inverts; §4 turns "84% inverted" into a named accounting of the other 16%, §4a shows the near-expiry cliff, §4b shows the sub-intrinsic rows are deep-ITM American contracts hitting a European floor. **§5 corrected an assumption this task started with:** the rate was expected to be worth "a fraction of a vol point", and it is 1.28 at the median but 5.69 at p95 and 24.96 at worst (over the 6,229 rows invertible at both 0% and 4%) — deep ITM, where the discounted strike moves the intrinsic floor while vega is ~0. That finding is now in the PRD's OQ-2 entry, SPEC §12 and a test. **Amended after T-44:** the notebook now ships **with stored outputs** (matching notebook 01 — the docs cite §5 as the source for the sensitivity figures, which was only true if you ran it), and §5 prints **both** row sets side by side, because the first landing quoted the four-rate subset's tail (5.6 / 21.7) under a sentence describing the two-rate one (5.69 / 24.96). |
| T-44 | Adversarial review of the T-17/T-25 changes (23 agents over 7 risk dimensions, 28 candidates, 8 verified by two skeptics each, 5 survived — 4 of them the same defect found independently by 4 dimensions). **Two real defects in the shipped page, both invisible to the suite and to the session's own browser check because that check only looked at geometry.** (1) The IV panel's caption baked the build date's "170 of 216 inverted" and the listener never rewrote it — so 52 of 53 slider steps stated a false count, including the one date where the panel is legitimately empty and the caption therefore described a figure showing nothing. (2) `customdata` was not restyled either, so every point's hover named a build-date contract, blanking entirely on the 16 dates with more points than the build date had. Both are the T-42 defect class surviving in the parts of a figure that are **not** its geometry; the payload now carries `ric` and `note` per date. Also found: `spread_heatmap`'s caption was **clipped in the shipped page** (pre-existing, y=1.10 against a 30px tile margin) and T-17's new caption-fit test green-lit it by modelling the anchor but not the text box — the test was certifying the defect it was written to catch. Plus two vacuous assertions in `test_iv.py` (`named <= len(holes)` and `max > median`, both true by construction) and the rate-sensitivity numbers quoted over three different row sets across the code comment, the docs and the notebook. All fixed and re-verified in Chromium; +3 tests. | NFR-2 | ✅ 2026-09-04 |
| T-45 | **[PO-directed redesign of FR-11's figure.]** The 3D IV scatter "just looks like a bunch of scattered points — I can't tell what it's trying to tell me", and it sat at the foot of the page, far from the surface it derives from. Now a **2D smile**: implied vol against `K / S`, one line+marker curve per expiry on a sequential `EXPIRY_RAMP`, at panel **[3]** beside mark-vs-print, directly under the hero. Spread moved to full width so the occupancy pair survives (`[6]`/`[7]`). **The flattening is the payoff** — on the busiest date the span of each expiry's smile collapses 81.8 → 70.6 → 40.7 → 21.5 → 14.6 → 13.4 vol points from the 7-day to the 42-day expiry. That term structure was in the 3D cloud all along and unreadable. Three engineering consequences: the trace ladder is fixed **panel-wide** (`panel_expiries`) because a per-date ladder would break the slider's restyle-by-index and would re-colour a curve on every step; a refused strike is `None` with `connectgaps=False`, because a line chart is the one form that can bridge a hole and invent a vol; and the caption counts **strikes**, not contract-days, so its number matches the dots a reader can count. Retired `SCENE_ASPECT_WIDE` / `SCENE_CAMERA_WIDE` / `WIDE_FIGURE_MARGIN` (dead once the 3D form went). Three more defects were visible only in the render — legend overprinting the caption, the assumptions line running off a 5-column tile, and a wrapped panel header leaving row 2 ragged by 17px. **Also caught: shortening the caption orphaned the CI publish grep**, which still looked for "DERIVED, NOT OBSERVED" and would have failed the deploy; the guard's phrases are now pinned to the page by a test and chosen to survive Plotly's JSON encoder (no "/", which ships as `/`). Driven in Chromium: 26 checks green. | FR-11, FR-8 | ✅ 2026-09-04 |
| T-43 | FR-10's K/S ruler now reaches the IV smile on the **published** page. Previously only the hero carried the `updatemenus` dropdown, so switching the ruler left the surface in K/S and the smile in dollars with nothing saying so. **[PO] directed the fix** and chose **Strike (K) as the shared default at load** — every other axis on the page is in dollar strikes at that point, so opening in dollars keeps the whole page in one unit. This is the first place two controls write the **same** property: the as-of slider picks the date, the axis menu picks the ruler, and both write the smile's `x`. T-16's disjoint-properties rule is about controls acting on a figure *directly*; here neither does — both feed the one listener, which holds the `(date, mode)` pair and is the sole writer. The frames payload therefore carries a complete smile per ruler (`iv: {strike: {...}, moneyness: {...}}`, +60 KB raw), including per-mode `show` and caption, because a date with no underlying close has no K/S ruler at all. **A defect found only by driving it:** the listener's `current` date started `null`, so toggling the ruler *before* touching the slider re-read whichever date happened to be first in the payload — the smile silently jumped from 90 points to 64. Seeded with the build date. Also fixed a double inversion (`asof_frames` was solving the same panel twice per date, once per ruler) and a page test whose fixed 3000-character slice had stopped covering the fail-safe guards it asserts. 18 Chromium checks green, including the invariant that matters: switching the ruler moves the x values and leaves all 90 vols untouched. | FR-10, FR-11, AD-5 | ✅ 2026-09-04 |
| T-47 | **Make the page hold its shape at every width** (PO, 2026-09-04): "can we make all the elements on the page dynamic so they don't shift around on different screen sizes? both on the published page and the reflex page". A width audit over 14 viewports (1920 → 430px) found **131 layout defects**, including one at *every* width: panel [4] drew its caption **twice, on top of itself**, in the subplot titles' font — `update_layout(annotations=[...])` broadcasts a one-element list across every existing annotation, so the caption had overwritten both subplot titles since T-13. Root cause of the rest: a Plotly caption is one unwrappable line pinned to a fraction of a box whose pixel width changes with the viewport, sharing the band above the plot with a legend that GROWS as the figure narrows — the same defect this project had already shipped at five different widths. **[PO] chose captions-in-HTML plus a width floor, and a third grid band at 1400px.** Captions now travel in `layout.meta` (`with_caption` / `figure_caption`) and each page renders them as text that wraps; no figure draws any text above its plot any more, and the tokens that used to position one — `CAPTION_Y*`, `LEGEND_ROW`, `LEGEND_BOX_PAD`, `LEGEND_ENTRIES_PER_ROW`, `theme.caption()` — are gone with the arithmetic tests that policed them, replaced by a rule with no arithmetic in it: **the band above a plot is empty.** A figure is never rendered below `FIGURE_MIN_WIDTH` (its panel scrolls instead), because a 53-step slider and a seven-entry legend do not fit a phone however carefully they are placed. **And the Reflex app now renders the published page's stylesheet and class names** — it had *no breakpoints at all*, having restated the panel chrome as inline component props; `PANEL_STYLE` / `PANEL_HEADER_STYLE` are deleted. Verified in Chromium: **0 defects across all 14 widths** (was 131), the Reflex app switching 10 → 2 → 1 columns with no sideways scroll, and 20 FR-12 + 7 caption checks still green. | FR-8, AD-6 | ✅ 2026-09-04 |
| T-46 | **Adversarial review of the T-18 changes** (27 agents: 7 risk dimensions, then two skeptics per candidate — one reading the source, one required to *reproduce* the failure, burden of proof on the finding). 15 candidates, 10 verified, **8 survived (4 distinct defects)**, 2 refuted, 5 dropped unverified by the cap and triaged by hand afterwards. **The headline defect was mine and it was on the graded artifact only:** the published plane was sized over BOTH rights while the page opens with puts parked on the legend, and plotly's 3D bounds ignore a parked trace — so the wall alone stretched the price axis on **34 of 53 dates, up to 6.8x**, flattening the call cloud onto the floor of the box. It was found independently by four dimensions and neither skeptic could refute any of them. **My own browser check had certified it**: it compared the plane's top against the axis range *the plane itself had set*. A circular check is not a check. Also found: a zero-area wall on the five single-expiry dates (draws nothing, legend entry still lit); a test that asserted `<= 1` plane per step and so passed with **no planes at all** — seven injected defects left the suite green; and the "can never move an axis" claim, false in four documents. Fixed: `OPENING_RIGHT` now ties the plane's extents to what `_vis_for` lights, `_plane_extents` refuses a degenerate box, the caption follows the slider (five dates draw no plane), and the guards are mutation-tested — **8 of 8 injected defects caught**, where the shipped suite caught 5. Re-driven in Chromium over all 53 steps: 19 checks green, worst plane/cloud ratio 1.000. | NFR-2 | ✅ 2026-09-04 |
| T-18 | Spot plane overlay at K = S, toggleable | FR-12 | **✅ 2026-09-04** — see [PRD FR-12](PRD.md) for the acceptance record. A constant-x `go.Surface` (two columns at one strike — a plane is a degenerate sheet), spanning the DTE and price box of **the rows that are actually drawn** (corrected by T-46 — it shipped sized over both rights and stretched the axis), in a new `SPOT_PLANE` slate at 0.18 with no colorbar and no hover. **One plane per as-of date that can carry one** (48 of 53 — the last five days have a single expiry alive and no box to span), lit by the slider's existing visibility array rather than by a control that would have to read the slider's state; a `show_spot_plane` switch in the Reflex app, the legend on the page. **It composes with FR-10 for nothing**: `K = S` is the spot in dollars and exactly 1.00 in moneyness, so the axis menu carries one number per ruler for it — the plane and the K/S axis then say the same thing two ways. **No spot, no plane** (AD-9), the same rule that gives such a date no K/S ruler. Driven in Chromium on the built page: **25 checks green**, zero page and console errors. **One defect was visible only in the render, again** — the plane is the hero's seventh legend entry, the legend wrapped to two rows, and the caption printed over it (DESIGN-BRIEF §8's defect by a new route). Caption clearance is now row-count arithmetic (`LEGEND_ROW`, `LEGEND_BOX_PAD`, `CAPTION_Y_OVER_LEGEND_2`) instead of a constant, and the test does the sum. Also reconciled two pieces of drift found in SYSTEM-SPEC §8.3 on the way past: it still described `reflex export` import-time baking (superseded by T-41) and panel ids `osl-fig-1..6` (seven since T-17). +8 tests, 210 green. |

## M4 — Ship (Sep 04, before midnight)

| ID | Task | Maps to | Status |
|---|---|---|---|
| T-19 | Finalize CI: pytest → `build_preview.py` → Pages on push; green on clean clone | FR-9, NFR-4 | ◐ shipped 2026-08-31, verify once more before submission |
| T-20 | Incognito verification of the live Pages URL (figures, toggles, numbers, sentences) | FR-9 | ☐ |
| T-21 | README how-to-run section (alongside, never replacing, the assignment brief) + Canvas submission | FR-9 | ☐ |
| T-22 | Final sweep: PRD §12 definition-of-done + docs-lockstep audit | all | ◐ 2026-09-04 — **docs-lockstep half done.** Reconciled: ARCHITECTURE §4 (app no longer "bakes" figures — T-14; `build_preview.py` is the deliverable, not a fallback — T-41; the test list named 2 of 8 files) and §5 (wall #2 said `SETTLE`, it is `MARK` — T-32); PRD §3 (headed 2026-08-29; the pickle was listed as "does not exist yet"; G-3 and G-6 were still open but shipped in T-11 / T-8+T-9); RUNBOOK §5 (still claimed the supporting panels stay pinned to one date — false since T-42) and its smoke checklist; notebook 01 (13 cells still used `SETTLE`/`has_settle` and would have raised — **plus two cells that never compiled at all**, a real newline inside a string literal, predating this session; the notebook now runs top-to-bottom on the real panel, verified by executing every cell). **Still open:** the PRD §12 definition-of-done pass at submission. |
